[tox]
envlist =
    py{36,37,38,39}
    pypy3
    build
    cov
    fmt
    lint
    release

[testenv]
deps =
    .[test]
commands =
    pytest {posargs}

[testenv:build]
basepython = python3.7
deps =
    twine
    wheel
commands =
    python setup.py sdist bdist_wheel
    twine check dist/*

[testenv:cov]
deps =
    .[test]
    coverage[toml]
    pytest-cov
commands =
    pytest --cov {posargs:-vv}

[testenv:fmt]
basepython = python3.7
deps =
    autoflake
    black
    isort[pyproject]
commands =
    autoflake --in-place --recursive --remove-all-unused-imports --ignore-init-module-imports .
    isort --apply --recursive
    black setup.py test cleanpy

[testenv:lint]
basepython = python3.7
deps =
    codespell
    mypy>=0.761
    pylama
    pytype
commands =
    python setup.py check
    codespell cleanpy test README.rst -q2 --check-filenames -L temporaly
    mypy cleanpy setup.py --ignore-missing-imports --show-error-context --show-error-codes --python-version 3.6
    pylama
    pytype --keep-going --jobs 4 --disable import-error cleanpy

[testenv:release]
basepython = python3.7
deps =
    releasecmd>=0.3.1,<1
commands =
    python setup.py release --sign --tag-template '\{version\}' {posargs}
